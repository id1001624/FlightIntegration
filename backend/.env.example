# Flask 應用配置
# 複製此文件為 .env 並填入正確的值
# 重要提示：.env 文件包含敏感資訊，不應提交到版本控制系統

# ========== 資料庫配置 ==========
# SQL Server 連接資訊
DB_SERVER=your_server_name        # 例：localhost 或 server.example.com
DB_NAME=your_database_name        # 例：FlightBookingDB
DB_USER=your_database_username    # 例：sa 或 flight_user
DB_PASSWORD=your_password         # 資料庫用戶密碼

# ========== Flask 應用配置 ==========
# 安全密鑰（用於加密會話）
SECRET_KEY=generate_a_strong_random_key  # 可使用 os.urandom(24).hex() 生成

# Flask 環境設定
FLASK_ENV=development             # development 或 production
FLASK_APP=run.py                  # Flask 應用入口點
FLASK_DEBUG=1                     # 1 啟用調試模式，0 關閉調試模式

# ========== API 配置 ==========
# 如有需要連接第三方 API（如航空公司 API）
# AIRLINE_API_KEY=your_api_key
# PAYMENT_GATEWAY_SECRET=your_payment_secret

# ========== 郵件服務配置 ==========
# MAIL_SERVER=smtp.example.com
# MAIL_PORT=587
# MAIL_USERNAME=your_email@example.com
# MAIL_PASSWORD=your_email_password
# MAIL_USE_TLS=1