---
description: 
globs: 
alwaysApply: false
---
# 測試策略與規範

本文檔定義了臺灣航班整合系統的測試策略和實踐規範，確保代碼品質和功能穩定性。

## 1. 測試類型與範圍

### 1.1 單元測試 (Unit Tests)

- **目標**: 驗證獨立功能單元的正確性
- **範圍**: 
  - 所有業務邏輯函數 (`services/` 目錄下的方法)
  - 工具函數 (`utils/` 目錄)
  - 複雜的資料轉換或處理邏輯
- **框架**: pytest
- **位置**: `backend/tests/unit/`
- **命名規範**: `test_<被測模組名>.py`

### 1.2 整合測試 (Integration Tests)

- **目標**: 驗證組件間交互的正確性
- **範圍**:
  - API 端點功能 (`controllers/` 的端點)
  - 資料庫操作 (ORM 模型與資料庫的交互)
  - 外部 API 整合 (如 TDX API, FlightStats API)
- **框架**: pytest + Flask Test Client
- **位置**: `backend/tests/integration/`
- **命名規範**: `test_int_<被測功能>.py`

### 1.3 端對端測試 (E2E Tests)

- **目標**: 驗證完整用戶流程
- **範圍**:
  - 主要用戶流程 (搜索航班、篩選結果、查看詳情)
  - LINE Bot 互動流程
- **框架**: Selenium/Playwright (前端), pytest (後端互動)
- **位置**: `tests/e2e/`
- **命名規範**: `test_e2e_<流程名稱>.py`

## 2. 測試資料管理

### 2.1 測試資料庫

- 測試使用獨立於生產環境的測試資料庫
- 使用 SQLAlchemy 的 `create_all` 進行測試前的資料庫初始化
- 每次測試完成後重設資料庫狀態

### 2.2 測試資料夾組織

```
tests/
├── conftest.py            # 共享 fixtures 和測試配置
├── unit/                  # 單元測試
│   ├── test_search_service.py
│   └── test_utils.py
├── integration/           # 整合測試
│   ├── test_int_flight_api.py
│   └── test_int_db_operations.py
└── e2e/                   # 端對端測試
    └── test_e2e_flight_search.py
```

### 2.3 測試模擬 (Mocking)

- 使用 `pytest-mock` 或 `unittest.mock` 模擬外部依賴
- 外部 API 呼叫應使用 `VCR.py` 或手動模擬回應
- 資料庫操作可使用 SQLAlchemy 的記憶體資料庫或模擬

## 3. 測試覆蓋率

### 3.1 覆蓋率目標

- **核心業務邏輯 (`services/`)**: ≥ 90% 行覆蓋率
- **工具函數 (`utils/`)**: ≥ 85% 行覆蓋率
- **API 控制器 (`controllers/`)**: ≥ 75% 行覆蓋率
- **資料模型 (`models/`)**: ≥ 70% 行覆蓋率
- **整體後端**: ≥ 80% 行覆蓋率

### 3.2 覆蓋率監控

- 使用 `pytest-cov` 產生覆蓋率報告
- CI/CD 流程中整合覆蓋率檢查
- 對未達標的模組進行標記和優先補充

## 4. 測試實踐

### 4.1 測試命名與組織

- 測試函數名應清晰描述測試目的: `test_<功能>_<條件>_<預期結果>`
- 使用 `pytest.mark` 對測試進行分類: `@pytest.mark.api`, `@pytest.mark.slow`
- 相關測試應組織在同一個測試類下

### 4.2 測試驅動開發 (TDD)

- 鼓勵先編寫測試，再實現功能
- 功能實現後確保所有測試通過
- 重構後重新運行測試確保功能正確性

### 4.3 測試前置與清理

- 使用 `pytest` 的 fixtures 進行測試前置設定
- 確保每個測試都進行適當的資源清理
- 避免測試間的狀態依賴

## 5. 持續整合 (CI)

- 每次提交自動運行單元和整合測試
- Pull Request 必須通過所有測試才能合併
- 定期運行完整的端對端測試套件
- 測試失敗立即通知開發團隊

## 6. 前端測試策略

### 6.1 Vue 組件測試

- 使用 Vue Test Utils 和 Jest 進行組件測試
- 重點測試複雜的交互組件如 `SearchForm.vue`, `FilterPanel.vue`
- 確保關鍵用戶操作流程有測試覆蓋

### 6.2 前端單元測試

- 測試前端的工具函數和業務邏輯函數
- 驗證數據轉換和處理函數的正確性
- 測試前端狀態管理邏輯

## 7. 效能測試

- 對關鍵 API 端點進行負載測試
- 使用 `JMeter` 或 `Locust` 模擬高流量
- 監控響應時間和系統資源利用率
- 訂定可接受的效能基準

