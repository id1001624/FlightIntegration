---
description: git
globs: 
alwaysApply: false
---

# commit_rules.mdc

## Git 提交規範指南

本文檔提供 Git 提交規範的基本指導，用於維護一致的版本歷史和協作流程。

### 提交訊息格式

- **基本格式**:
  ```
  <類型>: <簡短描述>(請用繁體中文)

  <詳細描述>

  <引用相關問題>
  ```

- **類型前綴**:
  - `feat`: 新功能
  - `fix`: 錯誤修復
  - `docs`: 文檔變更
  - `style`: 代碼風格調整（不影響代碼運行）
  - `refactor`: 重構（既不修復錯誤也不添加功能）
  - `perf`: 性能優化
  - `test`: 添加或修改測試
  - `build`: 構建系統或外部依賴變更
  - `ci`: CI 配置和腳本變更
  - `chore`: 其他不修改源代碼或測試的變更

- **簡短描述**:
  - 使用現在時態，例如 "添加" 而非 "添加了"
  - 不要以大寫字母開頭
  - 不要在末尾加句號
  - 描述應不超過50個字符

### 分支管理規範

- **主要分支**:
  - `updated-ui`(這是我們現在在用的分支) `main` / `master`: 生產環境代碼
  - `develop`: 開發環境代碼
  - `feature/*`: 功能開發分支
  - `bugfix/*`: 問題修復分支
  - `hotfix/*`: 生產環境緊急修復分支

- **分支命名**:
  - 使用小寫字母
  - 使用連字符分隔詞語，例如 `feature/user-authentication`
  - 簡潔明了地描述功能或修復

### 提交前檢查

- 確保代碼已通過本地測試
- 檢查是否有未使用的代碼或調試輸出
- 確保不包含敏感信息或大型二進制文件
- 考慮將大的變更拆分為多個較小的提交

### 提交相關工作流

- **小而頻繁**:
  - 每個提交應關注單一變更
  - 避免在一個提交中混合多個不相關的變更

- **關聯問題**:
  - 在提交訊息中引用相關問題
  - 使用關鍵字自動關聯，例如 `fixes #123` 或 `closes #456`

- **重要功能合併**:
  - 使用拉取請求 (Pull Request) 進行代碼審查
  - 確保通過 CI/CD 檢查後再合併

### 提交示例

```
feat: 添加用戶註冊功能

實現了用戶註冊表單和後端驗證邏輯，包括:
- 表單字段驗證
- 密碼強度檢查
- 電子郵件確認流程

fixes #42
```

```
fix: 修復移動設備上的佈局問題

修復了在 iOS Safari 瀏覽器中表單元素錯位的問題。

closes #78
```

### 常見問題與解決方案

- **意外提交**:
  - 使用 `git reset HEAD~1` 撤銷最後一次提交
  - 使用 `git commit --amend` 修改最後一次提交

- **需要臨時保存工作**:
  - 使用 `git stash` 臨時保存更改
  - 使用 `git stash pop` 恢復更改

- **合併提交**:
  - 在推送前使用 `git rebase -i HEAD~n` 整理提交歷史

## 參考資源

- Conventional Commits: https://www.conventionalcommits.org/
- Git 工作流: https://www.atlassian.com/git/tutorials/comparing-workflows
- GitHub Flow: https://guides.github.com/introduction/flow/
