---
description: 
globs: project-cursor-rules.mdc
alwaysApply: false
---
# 資料庫與後端開發檢查規則

## Neon資料庫訊息
- 項目ID: red-mud-66444885
- 項目名稱: FlightIntegration
- 資料庫名稱: flight_integration
- 主分支ID: br-calm-waterfall-a1iw1eta

## 1. 資料庫結構驗證
- **1.1 欄位存在性檢查**
  - 在寫 SQL 查詢前，必須先確認表格結構
  - 使用 `run_sql` 或 model 檢查實際的欄位名稱
  - 特別注意常見錯誤欄位：`is_active`, `logo_url`, `country` 等
  - 沒有iata_code，airline_id、airport_id就是

- **1.2 表格關聯檢查**
  - 確認 JOIN 條件中使用的欄位在相應表格中存在
  - 檢查外鍵關係是否正確設置
  - 驗證表格別名(alias)使用的一致性

## 2. SQL 查詢規範
- **2.1 欄位引用規則**
  - 使用正確的表格別名（例如：`al.is_domestic` 而非 `f.is_domestic`）
  - 明確指定欄位來源表格，避免模糊引用
  - 對所有選擇的欄位進行存在性驗證

- **2.2 查詢優化檢查**
  - 確認 WHERE 條件中使用的欄位存在
  - 檢查排序（ORDER BY）使用的欄位
  - 驗證 GROUP BY 和聚合函數的正確性

## 3. Model 層檢查
- **3.1 Model 定義驗證**
  - 在修改查詢前先檢查 Model 類的定義
  - 確認所有引用的欄位在 Model 中有定義
  - 驗證關聯關係的正確性

- **3.2 Model 使用規範**
  - 優先使用 Model 的屬性而非直接 SQL 查詢
  - 確保 Model 的更新與資料庫結構同步
  - 檢查 Model 的序列化方法

## 4. API 端點檢查
- **4.1 回應格式驗證**
  - 確認 API 回應中的欄位與資料庫欄位對應
  - 檢查必要的資料轉換邏輯
  - 驗證 NULL 值處理

- **4.2 錯誤處理**
  - 實作適當的錯誤捕捉機制
  - 提供明確的錯誤訊息
  - 確保所有資料庫操作都有錯誤處理

## 5. 開發流程檢查
- **5.1 修改前檢查**
  ```python
  # 檢查表格結構
  print(Model.__table__.columns.keys())
  
  # 檢查關聯
  print(Model.__mapper__.relationships.keys())
  ```

- **5.2 修改後驗證**
  - 重啟服務
  - 測試修改的 API 端點
  - 確認錯誤訊息已解決

## 6. 常見問題解決方案
- **6.1 欄位不存在**
  - 檢查實際資料庫結構
  - 修改 SQL 查詢以使用正確的欄位
  - 考慮是否需要資料庫遷移

- **6.2 關聯錯誤**
  - 確認 JOIN 條件
  - 檢查表格別名使用
  - 驗證外鍵設置

這些規則將幫助我在處理資料庫和後端相關問題時更加謹慎和系統化。每次修改程式碼時，我都會按照這個清單進行檢查，以避免重複發生相同的錯誤。
