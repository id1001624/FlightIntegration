---
description: 
globs: 
alwaysApply: false
---
# 資料同步詳細規則

本文檔定義了臺灣航班整合系統的資料同步機制與規範，確保航班數據的及時性、準確性和完整性。

## 1. 資料來源與優先級

### 1.1 主要資料來源

系統整合以下航班資料來源:

| 來源 | 資料類型 | 優先級 | 更新頻率 | 說明 |
|---|---|---|---|---|
| **TDX API** | 台灣航班 | 1 | 每小時 | 交通部運輸資料流通服務，提供台灣出發/抵達航班資訊 |
| **FlightStats API** | 國際航班 | 2 | 每4小時 | 提供詳細的國際航班信息，包括準點率和狀態 |
| **手動維護資料** | 基礎資料 | - | 視需要 | 機場、航空公司等基礎資料的手動更新 |

### 1.2 資料優先級策略

- 台灣地區航班優先使用 TDX API 資料
- 當 TDX API 無法提供數據時，退回到 FlightStats API
- 對於航班狀態和延誤信息，FlightStats API 優先
- 票價資料目前使用模擬數據，未來計劃整合真實票價 API

## 2. 同步機制與頻率

### 2.1 核心同步腳本: `sync_flight_data.py`

此腳本是數據同步的主要工具，負責從各來源獲取並更新航班資料:

```
# 執行完整同步 (包括航班和機場資料)
python -m backend.app.scripts.sync_flight_data

# 僅同步航班資料
python -m backend.app.scripts.sync_flight_data flights-only

# 僅同步特定路線
python -m backend.app.scripts.sync_flight_data flights -d TPE -a NRT --limit 10
```

### 2.2 自動化同步頻率(目前還不用自動化，先用模擬或手動，免費方案關係)

| 同步任務 | 頻率 | 執行時間 | 目標表格 |
|---|---|---|---|
| 台灣出發航班 | 每2小時 | 整點 | `flights` |
| 國際熱門航線 | 每4小時 | 02:00, 06:00, 10:00, 14:00, 18:00, 22:00 | `flights` |
| 歷史航班清理 | 每日 | 03:00 | `flights` (刪除過期資料) |
| 價格數據更新 | 每日 | 01:00 | `ticket_prices` |
| 機場資料更新 | 每月 | 每月1日 00:30 | `airports` |
| 航空公司資料 | 每月 | 每月1日 01:00 | `airlines` |

### 2.3 觸發機制

同步可以通過以下方式觸發:

1. **排程觸發**: 使用 Windows Task Scheduler 或 Cron Jobs 定期執行
2. **手動觸發**: 管理者通過命令行手動執行腳本
3. **API 觸發**: 透過受保護的管理 API 端點觸發 (`/api/admin/sync`)
4. **按需觸發**: 當用戶搜索特定航線且無資料時自動觸發

## 3. `sync_flight_data.py` 核心邏輯

### 3.1 流程概述

腳本執行以下步驟:

1. 初始化連接和日誌
2. 根據命令行參數決定同步範圍和目標
3. 驗證所需 API 憑證和參數
4. 從目標 API 獲取數據
5. 轉換數據為統一格式
6. 更新資料庫 (新增/更新記錄)
7. 記錄同步結果和統計

### 3.2 主要函數與職責

腳本中關鍵函數職責:

```python
def main():
    """主入口，處理命令行參數並調用相應函數"""
    
def sync_flights(departure=None, arrival=None, date=None, limit=None):
    """同步特定條件的航班資料"""
    
def sync_all_taiwan_departures():
    """同步所有從台灣出發的熱門航線"""
    
def sync_airports():
    """同步機場基本資料"""
    
def sync_airlines():
    """同步航空公司基本資料"""
    
def cleanup_old_flights(days=30):
    """清理超過指定天數的歷史航班資料"""
```

## 4. 資料處理與轉換

### 4.1 資料標準化

來自不同來源的數據需要標準化處理:

```python
def standardize_flight_data(flight_data, source):
    """將不同來源的航班數據轉換為標準格式"""
    if source == "tdx":
        # TDX API 數據標準化
        return {
            "flight_number": flight_data.get("FlyNumber"),
            "departure_airport": flight_data.get("DepartureAirportID"),
            "arrival_airport": flight_data.get("ArrivalAirportID"),
            "scheduled_departure_time": parse_datetime(flight_data.get("ScheduleDepartureTime")),
            "scheduled_arrival_time": parse_datetime(flight_data.get("ScheduleArrivalTime")),
            "airline_id": flight_data.get("AirlineID"),
            # ... 其他字段
        }
    elif source == "flightstats":
        # FlightStats API 數據標準化
        return {
            "flight_number": f"{flight_data.get('carrier')}{flight_data.get('flightNumber')}",
            "departure_airport": flight_data.get("departureAirportFsCode"),
            "arrival_airport": flight_data.get("arrivalAirportFsCode"),
            "scheduled_departure_time": parse_flightstats_time(flight_data.get("departureTime")),
            "scheduled_arrival_time": parse_flightstats_time(flight_data.get("arrivalTime")),
            "airline_id": flight_data.get("carrier"),
            # ... 其他字段
        }
```

### 4.2 資料合併策略

當從多個來源獲取同一航班資料時:

1. 使用航班號、日期、出發/到達機場作為唯一標識
2. 依照來源優先級決定採用哪個來源的資料
3. 對於特定欄位 (如航班狀態、登機口)，可採用高優先級來源數據
4. 同時保留最後更新時間和資料來源資訊

```python
def merge_flight_data(existing_flight, new_flight, new_source_priority):
    """合併現有航班資料與新獲取的資料"""
    # 如果新資料來源優先級高於現有資料，則完全替換
    if new_source_priority > existing_flight.source_priority:
        return new_flight
    
    # 否則，僅更新特定欄位
    merged = existing_flight.copy()
    
    # 某些欄位始終採用最新數據
    merged["updated_at"] = datetime.utcnow()
    
    # 狀態資訊優先採用 FlightStats 數據
    if new_source_priority == get_source_priority("flightstats"):
        merged["status"] = new_flight.get("status", merged.get("status"))
        merged["gate"] = new_flight.get("gate", merged.get("gate"))
        merged["terminal"] = new_flight.get("terminal", merged.get("terminal"))
    
    return merged
```

## 5. 錯誤處理與重試機制

### 5.1 API 錯誤處理

處理外部 API 錯誤的策略:

```python
def fetch_data_with_retry(url, params, headers, max_retries=3, backoff_factor=2):
    """從 API 獲取數據，支持重試機制"""
    retry_count = 0
    last_error = None
    
    while retry_count < max_retries:
        try:
            response = requests.get(url, params=params, headers=headers, timeout=30)
            response.raise_for_status()
            return response.json()
        except requests.RequestException as e:
            last_error = e
            wait_time = backoff_factor ** retry_count
            logger.warning(f"API 請求失敗 (重試 {retry_count+1}/{max_retries}): {str(e)}, 等待 {wait_time}s")
            time.sleep(wait_time)
            retry_count += 1
    
    # 所有重試失敗
    logger.error(f"API 請求最終失敗: {str(last_error)}")
    raise APIRequestFailedError(f"API 請求失敗: {str(last_error)}")
```

### 5.2 資料庫錯誤處理

同步過程中的資料庫操作錯誤處理:

```python
def safe_db_operation(operation_func, *args, **kwargs):
    """安全執行資料庫操作，處理錯誤並支持事務"""
    try:
        with db.session.begin():
            result = operation_func(*args, **kwargs)
            return result
    except SQLAlchemyError as e:
        db.session.rollback()
        logger.error(f"資料庫操作失敗: {str(e)}")
        raise DatabaseOperationError(f"資料庫操作失敗: {str(e)}")
    except Exception as e:
        db.session.rollback()
        logger.error(f"未知錯誤: {str(e)}")
        raise
```

### 5.3 特殊情況處理

針對特定錯誤情況的處理策略:

| 錯誤情況 | 處理策略 |
|---|---|
| API 配額用盡 | 降低請求頻率，等待下一個配額週期，優先同步熱門航線 |
| API 認證失敗 | 嘗試重新取得授權令牌，短期內切換到備用資料來源 |
| 資料庫連線中斷 | 在本地緩存數據，建立連接後批量提交 |
| 資料格式變更 | 記錄不兼容數據，發送告警，降級到可相容的數據處理邏輯 |

## 6. 資料緩存與效能優化

### 6.1 API 緩存

使用 `api_cache` 表存儲 API 回應，減少重複請求:

```python
def get_cached_api_response(endpoint, params_hash, max_age_minutes=60):
    """獲取緩存的 API 回應"""
    cache_entry = ApiCache.query.filter_by(
        endpoint=endpoint,
        params_hash=params_hash
    ).first()
    
    if cache_entry and cache_entry.is_valid(max_age_minutes):
        logger.debug(f"使用緩存的 API 數據: {endpoint}")
        return json.loads(cache_entry.response_data)
    
    return None

def cache_api_response(endpoint, params_hash, response_data):
    """緩存 API 回應"""
    try:
        # 檢查是否存在現有緩存條目
        cache_entry = ApiCache.query.filter_by(
            endpoint=endpoint,
            params_hash=params_hash
        ).first()
        
        if cache_entry:
            # 更新現有條目
            cache_entry.response_data = json.dumps(response_data)
            cache_entry.updated_at = datetime.utcnow()
        else:
            # 創建新條目
            cache_entry = ApiCache(
                endpoint=endpoint,
                params_hash=params_hash,
                response_data=json.dumps(response_data),
                created_at=datetime.utcnow(),
                updated_at=datetime.utcnow()
            )
            db.session.add(cache_entry)
        
        db.session.commit()
    except Exception as e:
        db.session.rollback()
        logger.error(f"緩存 API 回應失敗: {str(e)}")
```

### 6.2 批量數據處理

對於大量數據，採用批量處理提高效能:

```python
def bulk_upsert_flights(flight_data_list, batch_size=100):
    """批量更新/插入航班數據"""
    total_records = len(flight_data_list)
    total_batches = (total_records + batch_size - 1) // batch_size
    
    inserted = 0
    updated = 0
    
    for batch_index in range(total_batches):
        start_idx = batch_index * batch_size
        end_idx = min(start_idx + batch_size, total_records)
        batch = flight_data_list[start_idx:end_idx]
        
        try:
            with db.session.begin():
                for flight_data in batch:
                    # 嘗試查找現有記錄
                    existing = Flight.query.filter_by(
                        flight_id=flight_data["flight_id"]
                    ).first()
                    
                    if existing:
                        # 更新現有記錄
                        for key, value in flight_data.items():
                            if key != "flight_id" and value is not None:
                                setattr(existing, key, value)
                        updated += 1
                    else:
                        # 插入新記錄
                        flight = Flight(**flight_data)
                        db.session.add(flight)
                        inserted += 1
            
            logger.info(f"批次 {batch_index+1}/{total_batches} 完成處理")
        except Exception as e:
            db.session.rollback()
            logger.error(f"批次 {batch_index+1} 處理失敗: {str(e)}")
            # 可以選擇重試或繼續下一批次
    
    return {"inserted": inserted, "updated": updated}
```

## 7. 監控與通知

### 7.1 同步任務日誌

詳細記錄同步任務執行情況:

```python
def log_sync_task(task_type, status, details=None):
    """記錄同步任務執行情況"""
    sync_log = SyncLog(
        task_type=task_type,
        status=status,
        start_time=details.get("start_time") if details else None,
        end_time=details.get("end_time") if details else None,
        records_processed=details.get("records_processed", 0),
        records_updated=details.get("records_updated", 0),
        records_inserted=details.get("records_inserted", 0),
        errors=details.get("errors", None),
        source=details.get("source", None)
    )
    
    db.session.add(sync_log)
    db.session.commit()
    
    logger.info(f"同步任務記錄: {task_type}, 狀態: {status}, 處理記錄: {details.get('records_processed', 0)}")
```

### 7.2 關鍵錯誤通知

針對同步過程中的關鍵錯誤發送通知:

```python
def send_sync_error_notification(error_type, message, severity="high"):
    """發送同步錯誤通知"""
    # 整合 Email, Slack 或 Line 通知
    if severity == "high":
        # 立即通知管理員
        send_urgent_notification(error_type, message)
    else:
        # 記錄到通知隊列，定期發送摘要
        queue_notification(error_type, message)
    
    logger.error(f"同步錯誤 ({severity}): {error_type} - {message}")
```

### 7.3 任務狀態追蹤

追蹤長時間運行的同步任務狀態:

```python
def update_sync_status(task_id, status, progress=None, message=None):
    """更新同步任務狀態"""
    sync_status = SyncStatus.query.get(task_id)
    
    if not sync_status:
        sync_status = SyncStatus(id=task_id)
        db.session.add(sync_status)
    
    sync_status.status = status
    sync_status.progress = progress
    sync_status.message = message
    sync_status.updated_at = datetime.utcnow()
    
    db.session.commit()
```

## 8. 自動化排程設置

### 8.1 Windows Task Scheduler 設置

使用以下步驟設置 Windows 定時任務:

1. 開啟 Windows Task Scheduler
2. 創建基本任務，設置觸發時間
3. 設置啟動程序為 `批次檔案路徑`
4. 創建以下批次文件:

```batch
@echo off
cd /d "C:\path\to\project"
call venv\Scripts\activate.bat
python -m backend.app.scripts.sync_flight_data flights-only
```

### 8.2 批次文件說明

項目中的自動更新批次文件:

1. **`update_flights_only.bat`**: 僅更新航班數據
2. **`auto_update_tonight.bat`**: 晚間執行完整同步
3. **`README_排程任務設置.txt`**: 詳細的排程任務設置指南

### 8.3 排程示例

在 Windows Task Scheduler 中創建以下任務:

```
任務名稱: FlightData_HourlySync
觸發器: 每天，每小時重複
操作: 啟動程序 C:\path\to\update_flights_only.bat
開始位置: C:\path\to\project

任務名稱: FlightData_NightlySync
觸發器: 每天, 01:00
操作: 啟動程序 C:\path\to\auto_update_tonight.bat
開始位置: C:\path\to\project
```

## 9. 資料同步管理界面

### 9.1 管理功能

在未來版本計劃實現的管理界面功能:

- 查看同步任務執行歷史和統計
- 手動觸發特定航線的資料同步
- 查看和管理 API 緩存
- 調整同步參數和頻率
- 監控外部 API 的可用性和響應時間

### 9.2 資料品質監控

監控資料質量的指標:

- 航班資料完整度 (必要欄位填充率)
- 數據來源分佈 (各資料來源的比例)
- 同步失敗率趨勢
- 資料更新時效性 (最後更新到現在的時間)

